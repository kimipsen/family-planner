version: '3.0'

networks:
  app-global:
    external: true

services:
  tools-api:
    container_name: familyplannerblazor
    image: familyplannerblazor:latest
    ports:
      - 7001:7001
    networks:
      - app-global